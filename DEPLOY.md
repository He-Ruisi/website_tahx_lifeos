# 🚀 Tahx 部署指南 (Firebase Hosting)

按以下步骤将本项目发布到全球可访问的域名：

### 1. 准备工作
确保你已经安装了 [Node.js](https://nodejs.org/)。

### 2. 安装 Firebase 工具
打开终端，运行：
```bash
npm install -g firebase-tools
```

### 3. 登录 Firebase
```bash
firebase login
```

### 4. 初始化项目 (可选，已提供配置文件)
如果你想在 Firebase 控制台手动创建项目，请访问 [Firebase Console](https://console.firebase.google.com/)，创建一个名为 `tahx-life-os` (或你自己喜欢的名字) 的项目。

然后修改 `.firebaserc` 中的 `default` 值为你的项目 ID。

### 5. 一键部署
在项目根目录下运行：
```bash
firebase deploy
```

### 6. 完成！
部署完成后，终端会显示类似 `https://tahx-life-os.web.app` 的链接。

---

## 💡 注意事项
- **ESM 支持**：本项目直接使用了浏览器原生的 ES Module 加载 `.tsx`，Firebase Hosting 作为静态托管服务完美支持这种结构。
- **域名**：你可以通过 Firebase 控制台绑定自定义域名。
- **Gemini API**：如果未来在该项目中启用 AI 逻辑，请确保在部署环境安全地管理你的 API Key。