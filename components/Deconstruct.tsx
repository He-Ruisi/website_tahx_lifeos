// This component has been integrated into the HeroBento Modals (Architecture Card).
// Keeping file empty or minimal to avoid build errors if imported elsewhere, 
// though App.tsx no longer imports it.
export default function Deconstruct() { return null; }